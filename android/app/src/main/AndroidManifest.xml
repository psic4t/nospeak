<?xml version="1.0" encoding="utf-8"?>
     <manifest xmlns:android="http://schemas.android.com/apk/res/android">

      <queries>
          <intent>
              <action android:name="android.intent.action.VIEW" />
              <category android:name="android.intent.category.BROWSABLE" />
              <data android:scheme="nostrsigner" />
          </intent>
      </queries>

       <!-- Permissions -->
       <uses-permission android:name="android.permission.INTERNET" />
       <uses-permission android:name="android.permission.CAMERA" />
       <uses-permission android:name="android.permission.RECORD_AUDIO" />
       <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
       <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
       <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
       <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
      <!-- For saving downloaded files to shared Documents on older Android -->
      <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
      <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
      <!-- For media cache on Android 13+ (API 33) -->
      <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
      <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
      <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />


    <!-- Required for specialUse + systemExempted foreground service on Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <!-- Required for ConnectivityManager.NetworkCallback (network change reconnect) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Keep the app alive during background messaging work -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Restart background messaging after reboot/update -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Allow requesting Doze whitelist (best-effort) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- For showing notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for FGS type 'phoneCall' on Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <!-- Also required for FGS type 'phoneCall' on Android 14+. Without this,
         startForeground(.., FOREGROUND_SERVICE_TYPE_PHONE_CALL) throws
         SecurityException at runtime: Android requires the caller to either
         declare MANAGE_OWN_CALLS or hold the ROLE_DIALER role. We use it as
         a self-managed call app (NIP-AC over Nostr, no ConnectionService).
         Normal-protection permission; granted at install. See
         https://developer.android.com/about/versions/14/changes/fgs-types-required#phone-call -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <!-- Required to launch the activity over the keyguard for incoming calls (Android 14+ runtime grant) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
 
    <application
         android:allowBackup="true"
         android:icon="@mipmap/ic_launcher"
         android:label="@string/app_name"
         android:roundIcon="@mipmap/ic_launcher_round"
         android:supportsRtl="true"
         android:theme="@style/AppTheme"
         android:networkSecurityConfig="@xml/network_security_config">
 
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
 
             <intent-filter>
                 <action android:name="android.intent.action.MAIN" />
                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>

             <!-- Inbound share target for media and text/URLs -->
             <intent-filter>
                 <action android:name="android.intent.action.SEND" />
                 <category android:name="android.intent.category.DEFAULT" />
                 <data android:mimeType="image/*" />
                 <data android:mimeType="video/*" />
                 <data android:mimeType="audio/*" />
                 <data android:mimeType="text/*" />
             </intent-filter>

             <!-- Direct Share shortcuts configuration -->
             <meta-data
                 android:name="android.app.shortcuts"
                 android:resource="@xml/shortcuts" />
 
         </activity>

 
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <receiver
            android:name=".BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
     </receiver>

         <!-- Native foreground service for Android background messaging -->
          <service
              android:name=".NativeBackgroundMessagingService"
              android:enabled="true"
              android:exported="false"
              android:foregroundServiceType="specialUse|systemExempted">
              <property
                  android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                  android:value="nostr-dm" />
          </service>

        <!-- Foreground service hosting the active voice call -->
        <service
            android:name=".VoiceCallForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

        <!-- Hang up action from the active-call notification -->
        <receiver
            android:name=".VoiceCallActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Decline action from the incoming-call notification -->
        <receiver
            android:name=".IncomingCallActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Lockscreen ringing screen for incoming voice calls.
             Targeted by IncomingCallNotification's full-screen intent. -->
        <activity
            android:name=".IncomingCallActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.IncomingCall"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|smallestScreenSize|screenLayout|uiMode|navigation|density" />

        <!-- Active-call screen for the native voice-call stack. Phase 2 of
             the add-native-voice-calls change. Reused theme + lockscreen
             flags from IncomingCallActivity so the visual transition from
             ringing to active is seamless. singleTask launchMode (rather
             than singleInstance) so the activity keeps its task affinity
             with MainActivity — useful so the user can swipe back to the
             chat after the call ends. -->
        <activity
            android:name=".ActiveCallActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.IncomingCall"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|smallestScreenSize|screenLayout|uiMode|navigation|density" />
    </application>
 
</manifest>

